You've been invited to manage DNS using Bound. In order to login, you'll need to login or create an account using <%= Bound.config.auth.description %>. To continue, just click the link below and you'll be guided through.

<%= Bound.config.web.protocol %>://<%= Bound.config.web.host %>/join/<%= @user.invite_token %>

If you've got any questions, contact the DNS/Bound admin.
